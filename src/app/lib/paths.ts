enum Paths {
  INDEX = '/',
  NOTES = 'notes',
  USERS = 'users',
  TODOS = 'todos',
  DASHBOARD = 'dashboard',
  LOGIN = 'login',
  REGISTER = 'register',
  CONTACTS = 'contacts',
}

export default Paths;
